{
  "name": "@msinternal/botframework-webchat-samples-root",
  "private": true,
  "workspaces": [
    "./01.getting-started/l.sharepoint-web-part",
    "./04.api/e.piping-to-redux",
    "./04.api/f.selectable-activity",
    "./04.api/g.chat-send-history",
    "./04.api/h.clear-after-idle",
    "./06.recomposing-ui/a.minimizable-web-chat",
    "./06.recomposing-ui/b.speech-ui",
    "./06.recomposing-ui/c.smart-display",
    "./06.recomposing-ui/d.plain-ui",
    "./06.recomposing-ui/e.extending-ui"
  ],
  "scripts": {
    "postaudit": "find . -name package.json -not -path \"./package.json\" -and -not -path \"*/node_modules/*\" -not -path \"*/07.advanced-web-chat-apps/*\" $(jq -r '.workspaces | map(\"-and -not -path \" + . + \"/package.json\") | join(\" \")' ./package.json) | xargs dirname | xargs -I {} sh -c 'cd {} && npm audit'",
    "build": "npm run build --workspaces",
    "bump": "npm run bump:workspaces && npm run bump:non-workspaces",
    "bump:non-workspaces": "find . -name package.json -not -path \"./package.json\" -and -not -path \"*/node_modules/*\" -not -path \"*/07.advanced-web-chat-apps/*\" $(jq -r '.workspaces | map(\"-and -not -path \" + . + \"/package.json\") | join(\" \")' ./package.json) | xargs dirname | xargs -I {} sh -c 'cd {} && npm run bump'",
    "bump:workspaces": "npm run bump --workspaces"
  }
}
