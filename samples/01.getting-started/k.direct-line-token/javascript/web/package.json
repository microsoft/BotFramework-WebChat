{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "author": "Microsoft Corporation",
  "license": "MIT",
  "scripts": {
    "bump": "npm install --legacy-peer-deps $(cat package.json | jq -r '((.dependencies, .devDependencies) | keys) - (.skipBump | keys) | .[]' | awk '{print $1 \"@latest\"}') && npm audit fix",
    "start": "node-dev --no-notify --respawn ."
  },
  "skipBump": {
    "node-fetch": "node-fetch@3 dropped supported of CommonJS"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "http-proxy": "^1.18.1",
    "node-fetch": "^2.6.7",
    "restify": "^8.6.1"
  },
  "devDependencies": {
    "node-dev": "^7.4.3"
  }
}
