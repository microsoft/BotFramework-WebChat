extends:
  - ../../../.eslintrc.react.yml

env:
  # This package is expected to run under <script>.
  browser: true

globals:
  global: true
  host: true
  Observable: true

  # We are using React from UMD.
  React: true

rules:
  # This package contains lots of timeout values.
  no-magic-numbers: off
