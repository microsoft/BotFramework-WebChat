// TODO: [P1] --headless seems not honored, thus, the easiest way is to adjust the height of the window manually based on Chrome version.
//            Look at /packages/test/harness/src/host/jest/allocateWebDriver.js.
const ADJUST_WINDOW_SIZE_HEIGHT = 139;

module.exports = { ADJUST_WINDOW_SIZE_HEIGHT };
