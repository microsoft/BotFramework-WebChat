import { makeCreateStyles } from '@msinternal/botframework-webchat-styles';

export const componentStyleContent = '@--COMPONENT-STYLES-CONTENT--@';

export default makeCreateStyles(componentStyleContent);
