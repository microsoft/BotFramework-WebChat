import React from 'react';

export default class MicrophoneButton extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <button>Mic</button>
    );
  }
}
