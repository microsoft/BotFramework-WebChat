import coreRenderWebChat from '../../../renderWebChat';
import { ReactWebChat } from '../reactWebChat/full';

const renderWebChat = coreRenderWebChat.bind(null, ReactWebChat);

export { renderWebChat };
